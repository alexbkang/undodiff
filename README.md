# undodiff
Neovim plugin that wraps the built-in `undotree-plugin` and `codediff.nvim` to show undo history as side-by-side diffs.

## Features
- Show undo history as side-by-side.
- TODO: inline view
- TODO: Optionally collapse large blocks of unchanged lines.
